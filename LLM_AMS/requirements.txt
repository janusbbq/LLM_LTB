# LLM_AMS: LangChain-driven natural-language interface for LTB AMS
# pip install -r requirements.txt
#
# Tested against:
#   - Python 3.12 (conda env: llm-ams)
#   - ltbams >= 1.0
#   - ANDES >= 2.0
#
# Optional commercial solvers (gurobipy, mosek, copt) can be installed
# manually after acquiring a license; AMS will detect them automatically
# via cvxpy.installed_solvers().

# --- LangChain / LangGraph stack ---
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-ollama>=0.2.0

# --- Core AMS / power system ---
ltbams>=1.0.0
andes>=2.0.0
cvxpy>=1.5
kvxopt>=1.3.2.1
numpy>=1.26
scipy>=1.11
pandas>=2.1
sympy>=1.6,!=1.10.0
matplotlib>=3.8
openpyxl>=3.1
psutil
cffi
pybind11

# --- Open-source MIP / MILP solvers (Phase 2 routines: UC, ED, etc.) ---
# HiGHS is included via SCIPY in cvxpy; pyscipopt provides SCIP/SCIP-MI.
highspy>=1.7.0
pyscipopt>=5.0.0

# --- LLM tooling ---
pydantic>=2.5
typing-extensions>=4.9
python-dotenv>=1.0
rich>=13.7
yaspin>=3.0
